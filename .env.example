# nginx が公開するポート（ホスト側）
PUBLIC_PORT=8080

# ホスト側の検索対象ルート（NASでは /volume1/creative_workspace）
HOST_SEARCH_ROOT=/volume1/creative_workspace

# コンテナ内で許可するディレクトリ（カンマ区切り）
# docker-compose.yml のマウント先と一致させること
ALLOWED_DIRECTORIES=/data/creative_workspace
